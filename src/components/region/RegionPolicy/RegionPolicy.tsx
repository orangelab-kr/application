import {View} from 'react-native';
import styled from 'styled-components/native';

export const RegionPolicy = styled(View)`
  margin: 30px 10px;
  flex-direction: row;
  justify-content: space-around;
  align-items: center;
`;
